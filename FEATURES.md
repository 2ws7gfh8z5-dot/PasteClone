# PasteClone 功能介绍

PasteClone 是一款原生 macOS 剪贴板历史管理器。它在后台自动记录复制过的文本、图片、文件和富文本，通过全局快捷键快速呼出历史面板，搜索、预览、固定和粘贴历史内容。支持按应用来源筛选、收藏分类、粘贴队列等高级功能，本地存储不上传数据。

**核心特性**：
- 自动记录剪贴板历史（文本、图片、文件、RTF、颜色）
- 全局快捷键快速呼出历史面板（默认 `⇧⌘V`，可自定义）
- 实时搜索、按应用来源筛选、固定重要内容
- 面板内快捷键：`⌘C` 复制、`⌘V` 粘贴、`⌘P` 固定、`⌘Delete` 删除
- 粘贴队列：按顺序逐项粘贴多个历史项
- 本地 JSON 存储，支持开机启动、历史容量自定义
- 完全免费，支持自定义捐款链接
