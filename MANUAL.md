# PasteClone 详细使用说明书

## 目录

1. [安装与运行](#安装与运行)
2. [基础使用](#基础使用)
3. [高级功能](#高级功能)
4. [偏好设置](#偏好设置)
5. [常见问题](#常见问题)
6. [技术信息](#技术信息)

---

## 安装与运行

### 从源码构建

1. 克隆项目仓库：
   ```bash
   git clone https://github.com/2ws7gfh8z5-dot/PasteClone.git
   cd PasteClone
   ```

2. 打开 Xcode 项目：
   ```bash
   open PasteClone.xcodeproj
   ```

3. 选择 `PasteClone` scheme 和本地 Mac 作为运行目标，点击"运行"（`⌘R`）。

4. 应用首次启动时会请求剪贴板访问权限，点击"允许"。

5. 应用启动后出现在菜单栏（顶部状态栏），不会出现在 Dock。

### 开机启动

1. 打开 PasteClone 菜单栏图标，选择"偏好设置"（`⌘,`）
2. 在"通用"标签页勾选"启动时自动运行"
3. 下次开机时应用会自动启动

---

## 基础使用

### 呼出历史面板

按全局快捷键呼出历史面板（默认 `⇧⌘V`）。面板会显示最近的剪贴板历史项，按时间倒序排列。

### 搜索历史

1. 呼出历史面板后，搜索框已自动聚焦
2. 输入关键词（支持模糊搜索）
3. 列表实时过滤显示匹配的项目
4. 按 `Enter` 或 `⌘V` 粘贴第一个匹配项

### 粘贴历史项

三种方式选择并粘贴：

1. **点击粘贴**：在面板中找到想要的项目，双击或右键选择"粘贴"
2. **键盘粘贴**：
   - 用 `↑` `↓` 导航列表，按 `Enter` 或 `⌘V` 粘贴
   - 按 `⌘1` 到 `⌘9` 快速选择对应位置的项目
3. **复制后手动粘贴**：按 `⌘C` 复制选中项到系统剪贴板，在目标应用手动按 `⌘V`

### 固定重要内容

1. 在历史面板中选择要固定的项目
2. 按 `⌘P` 或右键选择"固定"
3. 该项目会移动到列表顶部，带有星标标记
4. 再按一次 `⌘P` 可取消固定

### 删除历史项

1. 在面板中选中要删除的项目
2. 按 `Delete` 或 `⌘Delete`，或右键选择"删除"
3. 项目从历史记录中永久删除

---

## 高级功能

### 按应用来源筛选

历史面板顶部显示各应用来源的标签。点击标签可只显示该应用复制过的内容，点击"全部"恢复显示所有历史。

### 粘贴队列

如果需要连续粘贴多个历史项：

1. 在历史面板中依次选中要粘贴的项目
2. 右键选择"加入粘贴队列"（或 `⌘+` 快捷键，如已配置）
3. 打开目标应用文本框
4. 按 `⌘V` 逐项粘贴队列中的内容
5. 粘贴完成后队列自动清空

### 预览各类型内容

- **文本**：直接在列表中显示文本摘要
- **图片**：显示缩略图，鼠标悬停可预览全图
- **文件**：显示文件名和路径，可直接从面板拖拽到其他应用
- **富文本（RTF/RTFD）**：显示格式化文本摘要
- **颜色**：显示颜色方块和 HEX 码

---

## 偏好设置

### 通用

- **启动时自动运行**：应用在每次开机时自动启动（后台运行，不显示窗口）
- **历史保留数量**：设置保存的最大历史项数（默认 100，范围 10-1000）
- **在状态栏显示 PasteClone**：勾选后应用图标始终显示在菜单栏

### 快捷键

- **全局呼出快捷键**：选择 `⇧⌘V`（默认）、`⌥Space` 或 `⌃⌥V`
- 更改后点击"保存"，立即生效

### 支持开发

- **捐款链接**：点击"打开"访问 GitHub Sponsors
- **自定义捐款链接**：在文本框输入自己的链接（如支付宝、微信等），点击"保存"

---

## 常见问题

### Q: PasteClone 记录的历史数据存储在哪里？

A: 历史数据保存在本地 JSON 文件：
```
~/Library/Application Support/PasteClone/history.json
```

### Q: 为什么某些应用粘贴不生效？

A: 某些特殊应用（如密码管理器、安全应用）可能不接受模拟键盘事件。解决办法：
1. 在 PasteClone 面板中复制内容（`⌘C`）
2. 在目标应用手动按 `⌘V`

### Q: 能否在 iCloud 云同步历史记录？

A: 当前版本不支持云同步。所有数据保存在本机，跨设备不同步。

### Q: 应用会上传剪贴板数据吗？

A: 不会。PasteClone 完全离线运行，所有数据仅保存在本机，不连接任何远程服务。

### Q: 如何清空所有历史记录？

A: 方法 1（简单）：在偏好设置中将历史保留数量改为 1，再复制任意内容，自动清空。
方法 2（完全）：删除文件 `~/Library/Application Support/PasteClone/history.json`，重启应用。

### Q: 为什么面板有时候出现在屏幕边缘？

A: 如果历史面板超出屏幕边界，会自动调整位置。下个版本会改进位置算法。

---

## 技术信息

### 系统要求

- macOS 14 Sonoma 或更高版本
- Apple Silicon 或 Intel Mac
- 2 GB 磁盘空间（存储历史）

### 项目架构

```
PasteClone/
├── Models/
│   ├── ClipboardItem.swift      # 历史项数据模型
│   └── ClipboardStore.swift     # 数据存储与持久化
├── Services/
│   ├── ClipboardMonitor.swift   # 后台剪贴板监听
│   ├── HotkeyManager.swift      # 全局快捷键注册
│   └── HotkeyConfiguration.swift # 快捷键配置
├── Views/
│   ├── HistoryPanelView.swift   # 历史面板 UI
│   ├── HistoryRow.swift         # 单行组件
│   ├── PreferencesView.swift    # 设置界面
│   └── Theme.swift              # 设计主题
└── PasteCloneApp.swift          # 应用入口
```

### 编译命令

```bash
xcodebuild -project PasteClone.xcodeproj \
  -scheme PasteClone \
  -configuration Debug \
  -sdk macosx build \
  CODE_SIGNING_ALLOWED=NO
```

### 无第三方依赖

项目仅使用 macOS 原生框架（SwiftUI、AppKit），无 CocoaPods、SPM 等外部依赖。

---

## 反馈与支持

- **问题报告**：https://github.com/2ws7gfh8z5-dot/PasteClone/issues
- **邮件联系**：15665874885@163.com
- **GitHub Sponsors**：支持开发，感谢！

---

**版本**：1.0.0 · 最后更新：2026-08-26
