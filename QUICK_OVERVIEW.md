# PasteClone 功能速览

## 是什么？
一款原生 macOS 剪贴板历史管理器，自动记录复制过的内容（文本、图片、文件、富文本），通过快捷键快速搜索和粘贴。

## 核心功能
| 功能 | 说明 |
|------|------|
| 自动记录 | 后台记录所有复制内容（文本、图片、文件、RTF、颜色） |
| 全局快捷键 | `⇧⌘V` 呼出历史面板（可自定义为 `⌥Space` 或 `⌃⌥V`） |
| 快速搜索 | 实时模糊搜索历史内容 |
| 按应用筛选 | 只显示特定应用复制的内容 |
| 固定收藏 | `⌘P` 标记重要内容为收藏 |
| 快速粘贴 | `Enter` 或 `⌘V` 粘贴，`⌘C` 复制 |
| 粘贴队列 | 连续粘贴多个历史项 |
| 本地存储 | JSON 文件保存，无云同步、无账号、无数据上传 |
| 开机启动 | 可配置开机自启动 |

## 快捷键速查

**全局快捷键**
- `⇧⌘V` - 呼出历史面板（默认）

**面板内快捷键**
- `Enter` / `⌘V` - 粘贴选中项
- `⌘C` - 复制选中项
- `⌘P` - 固定/取消固定
- `Delete` / `⌘Delete` - 删除
- `⌘1-9` - 快速选择 1-9 位置的项
- `↑` / `↓` - 上下导航
- `Esc` - 关闭面板
- `⌘F` - 聚焦搜索框
- `⌘,` - 打开偏好设置

## 一句话说明
复制一段文字 → 按 `⇧⌘V` → 搜索历史 → 按 `Enter` 粘贴。完成。

## 配置位置
- 历史数据：`~/Library/Application Support/PasteClone/history.json`
- 最大历史数：偏好设置中可设（默认 100 项）
- 快捷键：偏好设置中可切换

## 系统要求
- macOS 14 Sonoma 或更高
- Apple Silicon / Intel Mac

## 联系方式
- 邮件：`15665874885@163.com`
- GitHub：https://github.com/2ws7gfh8z5-dot/PasteClone
- 支持：GitHub Sponsors

