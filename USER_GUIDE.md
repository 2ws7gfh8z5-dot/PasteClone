# PasteClone 用户使用说明书

## 1. PasteClone 是什么

PasteClone 是一个常驻菜单栏的 macOS 剪贴板历史工具。普通的系统剪贴板通常只保留最后一次复制内容；PasteClone 会在后台保存最近复制的多条内容，让你可以搜索、重复使用和整理过去复制过的资料。

PasteClone 的数据默认只保存在本机，不需要注册账号，也不会主动把剪贴板内容上传到网络。

## 2. 系统要求与安装

- macOS 14 Sonoma 或更高版本
- Xcode 15 或更高版本（仅源码构建需要）
- Apple Silicon 或 Intel Mac

### 从源码运行

1. 下载或克隆仓库。
2. 双击 `PasteClone.xcodeproj`，或在终端进入项目目录后运行 `open PasteClone.xcodeproj`。
3. 在 Xcode 顶部选择 `PasteClone` scheme 和 `My Mac`。
4. 点击 Run（`⌘R`）。
5. 首次启动后，在菜单栏找到 PasteClone 图标。

命令行构建：

```bash
xcodebuild -project PasteClone.xcodeproj \
  -scheme PasteClone -configuration Debug -sdk macosx \
  build CODE_SIGNING_ALLOWED=NO
```

应用是菜单栏程序，正常情况下不会出现在 Dock 中。退出应用请从菜单栏菜单选择“退出 PasteClone”。

## 3. 第一次使用

1. 启动 PasteClone。
2. 在任意应用中复制文字、图片、文件或富文本。
3. 等待不到一秒，PasteClone 会自动记录。
4. 按 `⇧⌘V`，历史面板会显示最近内容。
5. 点击一条记录后按 `Enter`，内容会写回系统剪贴板并发送 `⌘V` 到原来的前台应用。

如果没有看到记录，请确认应用仍在运行，并再次进行一次复制。PasteClone 不会补抓启动前已经存在的系统剪贴板历史。

## 4. 支持的内容类型

| 类型 | 记录内容 | 粘贴行为 |
|---|---|---|
| 文本 | 纯文本字符串 | 写入文本并自动粘贴 |
| 图片 | PNG 预览数据 | 写入图片并自动粘贴 |
| 文件 | 一个或多个文件 URL | 写入文件 URL 并自动粘贴 |
| 富文本 | RTF/RTFD 数据及纯文本预览 | 尽量保留富文本格式 |
| 颜色 | 颜色对象（如应用提供） | 写回颜色对象 |

不同应用提供的剪贴板格式可能不同；如果目标应用不支持对应格式，系统会使用它能识别的格式。

## 5. 历史面板

### 打开与关闭

- 菜单栏图标 → “打开剪贴板”
- 全局快捷键 `⇧⌘V`
- 点击右上角关闭按钮
- 面板获得焦点时按 `Esc`

### 搜索

在顶部搜索框输入关键词，列表会即时筛选：

- 文本内容
- 文件名
- 来源应用名称

按 `⌘F` 可直接聚焦搜索框。搜索框中按 `Enter` 会粘贴当前选中记录。

### 选择与粘贴

- 单击：选中记录
- `↑` / `↓`：上下移动选择
- `Enter`：粘贴选中记录
- 双击：直接粘贴该记录
- 右键 → “粘贴”：直接粘贴该记录
- `⌘1` 至 `⌘9`：选择列表中第 1 至第 9 条记录

列表会把固定记录放在普通记录之前。搜索或切换收藏分类后，选择会自动调整到当前可见列表。

## 6. 固定、删除与收藏

### 固定

固定后，记录会排在列表顶部，并且不会被“清除历史”删除。

- 鼠标移到记录上，点击图钉按钮
- 右键 → “固定”或“取消固定”
- 选中记录后按 `⌘P`

### 删除

删除一条记录：

- 鼠标移到记录上，点击垃圾桶
- 右键 → “删除”
- 选中记录后按 `⌘Delete`

删除是立即生效的。若要清理普通历史，点击底部“清除历史”；该操作保留所有固定记录。

### 收藏分类

1. 鼠标移到记录上，点击文件夹按钮，或右键打开“收藏到”。
2. 选择已有分类，或选择“新建收藏”。
3. 底部文件夹菜单可按分类筛选。
4. 选择“显示全部”恢复完整列表。
5. 在同一菜单中可以移除某个收藏分类；移除分类不会删除记录，只会取消分类。

## 7. 粘贴队列

粘贴队列适合把多条内容连续输入到同一个目标应用：

1. 鼠标移到记录上，点击“加入队列”，或右键选择“加入粘贴队列”。
2. 重复加入需要的记录。
3. 打开面板，点击底部“粘贴队列”。
4. PasteClone 会从队列头部取出一条，写入剪贴板并自动发送 `⌘V`。
5. 重复打开并点击，直到队列清空。

队列按加入顺序执行，不会改变历史记录本身。当前队列数量会显示在底部按钮旁。

## 8. 偏好设置

通过菜单栏菜单中的“偏好设置”打开设置窗口，也可以在历史面板按 `⌘,`。

### 保留条目

可选择保留 `100`、`500`、`1000` 或 `2000` 条普通记录。固定记录会优先保留，不受普通记录清理影响。

### 开机启动

打开“开机启动”后，macOS 会在登录时启动 PasteClone。关闭开关即可注销登录启动项。如果系统拒绝更改，设置会恢复为系统实际状态。

### 全局热键

“全局热键（`⇧⌘V`）”默认开启。关闭后，快捷键不会在其他应用中呼出面板，但仍可通过菜单栏打开。重新打开开关会重新注册快捷键。

如果快捷键与其他软件冲突，请先关闭冲突软件的同一组合键，或暂时关闭 PasteClone 的全局热键。

## 9. 完整快捷键表

| 快捷键 | 作用 | 使用位置 |
|---|---|---|
| `⇧⌘V` | 打开/隐藏历史面板 | 全局 |
| `↑` | 选择上一条 | 历史面板 |
| `↓` | 选择下一条 | 历史面板 |
| `Enter` | 粘贴当前选中记录 | 历史面板/搜索框 |
| `Esc` | 关闭历史面板 | 历史面板 |
| `⌘1`–`⌘9` | 选择第 1–9 条 | 历史面板 |
| `⌘Delete` | 删除当前记录 | 历史面板 |
| `⌘P` | 固定/取消固定 | 历史面板 |
| `⌘F` | 聚焦搜索框 | 历史面板 |
| `⌘,` | 打开偏好设置 | 历史面板 |
| 双击记录 | 粘贴该记录 | 历史面板 |
| 右键记录 | 打开操作菜单 | 历史面板 |

## 10. 数据、安全与隐私

历史文件位于：

```text
~/Library/Application Support/PasteClone/history.json
```

文件由系统用户权限保护，但它包含你复制过的实际内容。请不要把该文件提交到 GitHub，也不要把它发送给他人。项目的 `.gitignore` 已忽略本地构建产物和用户数据；仓库只包含源代码和应用资源。

建议：

- 不要复制密码、私钥或一次性验证码后长期保留。
- 离开电脑前退出应用，或删除敏感记录。
- 共享电脑上使用前确认当前 macOS 用户可信。

## 11. 常见问题

### 复制了内容但列表没有更新

确认 PasteClone 正在运行，然后重新复制一次。应用只监听启动后的剪贴板变化，每 0.4 秒检查一次。

### 按 `⇧⌘V` 没反应

从菜单栏打开一次应用，确认“全局热键”开关已开启。如果其他应用占用了该组合键，请关闭冲突或改用菜单栏入口。

### 选中后没有自动粘贴

PasteClone 需要把 `⌘V` 发给当前前台应用。部分安全软件、终端、远程桌面或受保护输入框会阻止模拟键盘事件；此时重新打开面板，用 PasteClone 写回剪贴板后在目标应用手动按 `⌘V`。

### 历史记录太多

在偏好设置中降低“保留条目”，或点击“清除历史”。固定记录不会被清除。

### 图片或文件显示不完整

历史记录保存的是图片数据和文件 URL。原始文件被移动或删除后，PasteClone 无法恢复文件本身；图片会受系统剪贴板提供的数据格式影响。

## 12. 开发者说明

项目结构：

- `PasteClone/Models/`：剪贴板数据模型与本地存储
- `PasteClone/Services/`：剪贴板轮询和 Carbon 全局快捷键
- `PasteClone/Views/`：历史面板、历史行、设置和视觉主题
- `PasteClone/Resources/`：Info.plist、权限配置和图标资源

项目采用原生 SwiftUI/AppKit、XcodeGen 生成工程，不使用第三方依赖。构建验证：

```bash
xcodebuild -project PasteClone.xcodeproj \
  -scheme PasteClone -configuration Debug -sdk macosx \
  build CODE_SIGNING_ALLOWED=NO
```

## 13. 许可

仓库当前未声明开源许可证。除非仓库另行添加 LICENSE 文件，否则请不要默认将代码用于再发布或商业分发。
